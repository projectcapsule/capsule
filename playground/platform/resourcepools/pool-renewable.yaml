---
apiVersion: capsule.clastix.io/v1beta2
kind: ResourcePool
metadata:
  name: renewable
spec:
  quota:
    hard:
      limits.memory: 2Gi
      limits.cpu: "0"
      requests.cpu: "2"
      requests.memory: 2Gi
      requests.storage: "5Gi"
  selectors:
  - matchLabels:
      customer: renewable
