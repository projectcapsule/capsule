---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: breakrequests.capsule.clastix.io
spec:
  group: capsule.clastix.io
  names:
    kind: BreakRequest
    listKind: BreakRequestList
    plural: breakrequests
    singular: breakrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.reason
      name: Reason
      type: string
    - jsonPath: .status.review.verdict
      name: Verdict
      type: string
    - jsonPath: .status.active.from
      name: ActiveFrom
      priority: 10
      type: string
    - jsonPath: .status.active.until
      name: ActiveUntil
      priority: 10
      type: string
    - jsonPath: .status.request.duration
      name: Duration
      priority: 10
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Message
      priority: 10
      type: string
    - description: The number of managed resources
      jsonPath: .status.size
      name: Items
      type: integer
    - description: Age
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: BreakRequest is the Schema for the BreakRequests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BreakRequestSpec defines the desired state of BreakRequest.
            properties:
              duration:
                description: |-
                  The duration of this BreakRequest should be valid for.
                  If no duration was defined, the lifecycle is bound to the request itself -
                  if the request is deleted, it's the end of the duration.
                  The Request can also be Terminated by another automation via calling the ExpireRequest() API-Function.
                type: string
              params:
                description: Params the parameters to use for the template.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              reason:
                description: A reason on why the request is needed
                type: string
              requestor:
                description: Requesting actor for the access request.
                properties:
                  groups:
                    description: The groups the entity belongs to
                    items:
                      type: string
                    type: array
                  name:
                    description: The name of the entity
                    type: string
                  type:
                    description: The type of the entity
                    enum:
                    - User
                    - Group
                    - System
                    - ServiceAccount
                    type: string
                type: object
              startTime:
                description: |-
                  Optional point in time when the access should begin. Must be in the future.
                  If omitted, this is set to the current time. The Request must already be approved before the start time.
                format: date-time
                type: string
              template:
                description: Template references the template to use for this request.
                properties:
                  kind:
                    description: Kind of template being referenced.
                    enum:
                    - BreakRequestTemplate
                    - GlobalBreakRequestTemplate
                    type: string
                  name:
                    description: Name of the template being referenced.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - template
            type: object
          status:
            description: BreakRequestStatus defines the observed state of BreakRequest.
            properties:
              active:
                description: Shows timestamps between approval and termination of
                  the request.
                properties:
                  from:
                    format: date-time
                    type: string
                  until:
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions describes current operational state such as
                  readiness.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failure:
                description: |-
                  Failure describes a recoverable preflight or activation failure. RetryPhase
                  is the trusted lifecycle phase Capsule resumes after a successful retry.
                properties:
                  message:
                    description: Message contains the latest actionable failure returned
                      by Kubernetes.
                    type: string
                  reason:
                    description: Reason is the stable machine-readable Ready condition
                      reason.
                    type: string
                  retryPhase:
                    description: RetryPhase is the phase Capsule resumes after recovery
                      succeeds.
                    enum:
                    - Requested
                    - Approved
                    type: string
                  stage:
                    description: |-
                      Stage identifies whether the failure happened before review or while
                      activating an already approved request.
                    enum:
                    - Preflight
                    - Activation
                    type: string
                required:
                - message
                - reason
                - retryPhase
                - stage
                type: object
              keepUntil:
                description: |-
                  The time until which the BreakRequest should be retained after it expires (e.g. for auditing).
                  If unset, the BreakRequest can be deleted immediately after expiring.
                format: date-time
                type: string
              phase:
                enum:
                - Created
                - Requested
                - Pending
                - Denied
                - Approved
                - Active
                - Failed
                - Retrying
                - Expired
                type: string
              processedItems:
                description: List of resources managed by the API object.
                items:
                  description: Advanced Status Item for pin pointing items in tenants/namespaces.
                  properties:
                    group:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    origin:
                      type: string
                    status:
                      properties:
                        clusterScoped:
                          description: Indicates whether the referenced resource is
                            cluster-scoped.
                          type: boolean
                        created:
                          description: Indicates wether the resource was created or
                            adopted
                          type: boolean
                        lastApply:
                          description: |-
                            An opaque value that represents the internal version of this object that can
                            be used by clients to determine when objects have changed. May be used for optimistic
                            concurrency, change detection, and the watch operation on a resource or set of resources.
                            Clients must treat these values as opaque and passed unmodified back to the server.
                            They may only be valid for a particular resource or set of resources.

                            Populated by the system.
                            Read-only.
                            Value must be treated as opaque by clients and .
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    tenant:
                      type: string
                    version:
                      type: string
                  type: object
                type: array
              request:
                description: |-
                  Request contains the resolved template, execution identity, lifecycle
                  properties, and rendered resources presented for review.
                properties:
                  approvals:
                    description: |-
                      Approvals is the approval policy copied from the resolved template. It is
                      immutable after rendering so an in-flight request is reviewed against the
                      policy presented with its resource snapshot.
                    properties:
                      approvers:
                        description: |-
                          Approvers lists the subjects permitted to approve a request manually.
                          When omitted, any subject with permission to update the request status may
                          approve it.
                        items:
                          properties:
                            kind:
                              description: Kind of entity. Possible values are "User",
                                "Group", and "ServiceAccount"
                              enum:
                              - User
                              - Group
                              - ServiceAccount
                              type: string
                            name:
                              description: Name of the entity.
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                        type: array
                      auto:
                        description: |-
                          Auto automatically approves matching requests. Approvers are ignored for
                          automatic approvals.
                        type: boolean
                      conditions:
                        description: |-
                          Conditions contains CEL expressions evaluated as an OR list. At least one
                          expression must evaluate to true. When omitted, approval is unconditional.
                        items:
                          minLength: 1
                          type: string
                        type: array
                    type: object
                  duration:
                    type: string
                  impersonation:
                    description: |-
                      Impersonation is the resolved identity used for template context loading
                      and managed-resource actions. Capsule records its controller ServiceAccount
                      when no impersonation is configured.
                    properties:
                      name:
                        description: Name of the referent.
                        maxLength: 63
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      namespace:
                        description: Namespace of the referent.
                        maxLength: 253
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  keepFor:
                    pattern: ^([0-9]+(\.[0-9]+)?(s|m|h|d|w))+$
                    type: string
                  resources:
                    description: |-
                      Resources contains the fully rendered manifests prepared for this request.
                      These resources are the source of truth for server-side apply and pruning;
                      source templates and rendering context are never copied into the request.
                    items:
                      description: |-
                        RenderedResource is an execution-ready group of Kubernetes manifests
                        produced from a ResourceTemplate. It intentionally cannot contain a source
                        template so status consumers and reconcilers only observe concrete targets.
                      properties:
                        policy:
                          description: Policy controls how every rendered target is
                            created and managed.
                          properties:
                            creation:
                              default: Owner
                              description: |-
                                Creation controls how an existing target is handled. Owner requires the
                                resource to have been created by the applying controller and otherwise
                                returns an error. Merge adopts an existing resource when possible and
                                creates the resource when it does not exist.
                              enum:
                              - Owner
                              - Merge
                              type: string
                            deletion:
                              default: Remove
                              description: |-
                                Deletion controls what happens when the parent stops managing the target.
                                Remove deletes resources created for the parent and relinquishes adopted
                                resources. Orphan keeps the resource and removes Capsule's lifecycle
                                metadata. Removal is the default.
                              enum:
                              - Remove
                              - Orphan
                              type: string
                            force:
                              default: false
                              description: Force allows server-side apply to acquire
                                conflicting field ownership.
                              type: boolean
                            protect:
                              default: true
                              description: |-
                                Protect prevents users from changing or deleting the target through
                                admission while it is managed. Protection is enabled by default.
                              type: boolean
                          type: object
                        targets:
                          description: Targets are the fully rendered Kubernetes manifests.
                          items:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          minItems: 1
                          type: array
                      required:
                      - targets
                      type: object
                    type: array
                  startTime:
                    format: date-time
                    type: string
                  template:
                    description: Template identifies the exact template version used
                      to render the request.
                    properties:
                      kind:
                        description: Kind of template being referenced.
                        enum:
                        - BreakRequestTemplate
                        - GlobalBreakRequestTemplate
                        type: string
                      name:
                        description: Name of the template being referenced.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      resourceVersion:
                        description: ResourceVersion of the template used to render
                          the request resources.
                        type: string
                    required:
                    - kind
                    - name
                    - resourceVersion
                    type: object
                type: object
              review:
                description: Review refers to the subject that either approved or
                  denied the request
                properties:
                  message:
                    description: Message with the review
                    type: string
                  reviewer:
                    description: The Entity reviewing this request
                    properties:
                      groups:
                        description: The groups the entity belongs to
                        items:
                          type: string
                        type: array
                      name:
                        description: The name of the entity
                        type: string
                      type:
                        description: The type of the entity
                        enum:
                        - User
                        - Group
                        - System
                        - ServiceAccount
                        type: string
                    type: object
                  verdict:
                    description: The verdict made by the reviewing entity
                    enum:
                    - Pending
                    - Denied
                    - Approved
                    type: string
                type: object
              size:
                description: Number of managed resources.
                type: integer
              transitions:
                description: |-
                  Transitions is the chronological, append-only audit trail of lifecycle
                  changes. Conditions remain reserved for operational state such as Ready.
                items:
                  description: BreakRequestTransition records one authenticated lifecycle
                    transition.
                  properties:
                    actor:
                      description: |-
                        Actor is the authenticated user, ServiceAccount, or Capsule system actor
                        responsible for the transition. Group claims are deliberately not copied
                        into the audit trail.
                      properties:
                        name:
                          description: Name is the authenticated actor name.
                          type: string
                        type:
                          description: Type identifies the kind of authenticated actor.
                          enum:
                          - User
                          - Group
                          - System
                          - ServiceAccount
                          type: string
                      required:
                      - name
                      - type
                      type: object
                    eventTime:
                      description: |-
                        EventTime is set after Capsule emits the Kubernetes lifecycle event for
                        this transition.
                      format: date-time
                      type: string
                    message:
                      description: Message is the human-readable explanation of the
                        transition.
                      type: string
                    reason:
                      description: Reason is a stable machine-readable explanation
                        of the transition.
                      type: string
                    timestamp:
                      description: Timestamp is when the transition was requested
                        or performed.
                      format: date-time
                      type: string
                    type:
                      description: |-
                        Type identifies the lifecycle state entered by this transition. The
                        previous state can be derived from the preceding chronological entry.
                      enum:
                      - Created
                      - Requested
                      - Pending
                      - Denied
                      - Approved
                      - Active
                      - Failed
                      - Retrying
                      - Expired
                      type: string
                  required:
                  - actor
                  - reason
                  - timestamp
                  - type
                  type: object
                type: array
            required:
            - size
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
