---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: breakrequesttemplates.capsule.clastix.io
spec:
  group: capsule.clastix.io
  names:
    kind: BreakRequestTemplate
    listKind: BreakRequestTemplateList
    plural: breakrequesttemplates
    shortNames:
    - brt
    singular: breakrequesttemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.approvals.auto
      name: AutoApprove
      type: boolean
    - description: Age
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: BreakRequestTemplate is the Schema for namespaced BreakRequest
          templates.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BreakRequestTemplateSpec defines the desired state of a namespaced
              BreakRequestTemplate.
            properties:
              approvals:
                description: Approvals configures automatic and manual approval of
                  requests using this template.
                properties:
                  approvers:
                    description: |-
                      Approvers lists the subjects permitted to approve a request manually.
                      When omitted, any subject with permission to update the request status may
                      approve it.
                    items:
                      properties:
                        kind:
                          description: Kind of entity. Possible values are "User",
                            "Group", and "ServiceAccount"
                          enum:
                          - User
                          - Group
                          - ServiceAccount
                          type: string
                        name:
                          description: Name of the entity.
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    type: array
                  auto:
                    description: |-
                      Auto automatically approves matching requests. Approvers are ignored for
                      automatic approvals.
                    type: boolean
                  conditions:
                    description: |-
                      Conditions contains CEL expressions evaluated as an OR list. At least one
                      expression must evaluate to true. When omitted, approval is unconditional.
                    items:
                      minLength: 1
                      type: string
                    type: array
                type: object
              context:
                description: |-
                  Context loads additional Kubernetes resources for use by all resource targets and templates.
                  Resource reference fields may use parameters declared by ParamSchema.
                properties:
                  resources:
                    items:
                      properties:
                        apiVersion:
                          description: |-
                            API version, API group, or API group/version selector of the referent.

                            Empty APIVersion means the core Kubernetes API version "v1".
                            Use "*" to explicitly match all API groups and versions.

                            Examples:
                            - "" means core "v1".
                            - "v1" means core "v1".
                            - "apps" means any version in the "apps" API group.
                            - "apps/v1" means the "apps/v1" API group/version.
                            - "apps/*" means any version in the "apps" API group.
                          type: string
                        index:
                          description: Index to mount the resource in the template
                            context
                          type: string
                        kind:
                          description: |-
                            Kind of the referent.

                            Use "*" to match all kinds.
                          minLength: 1
                          type: string
                        name:
                          description: |-
                            Name of the values referent. This is useful
                            when you traying to get a specific resource
                          maxLength: 253
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace of the values referent.
                          type: string
                        optional:
                          default: true
                          description: Only relevant if name is set. If an item is
                            not optional, there will be an error thrown when it does
                            not exist
                          type: boolean
                        selector:
                          description: Selector which allows to get any amount of
                            these resources based on labels
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - kind
                      type: object
                    type: array
                type: object
              defaultDuration:
                description: The default duration of a BreakRequest referencing this
                  template.
                type: string
              impersonation:
                description: |-
                  Impersonation identifies the namespace-local ServiceAccount used for
                  context loading and every managed-resource action performed for requests
                  using this template. When omitted, the tenant default ServiceAccount from
                  CapsuleConfiguration is used. If neither is configured, Capsule uses its
                  controller identity.
                properties:
                  name:
                    description: Name of the referent.
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              keepFor:
                description: The duration a BreakRequest is retained after it expires
                  for auditing.
                pattern: ^([0-9]+(\.[0-9]+)?(s|m|h|d|w))+$
                type: string
              maxDuration:
                description: The maximum allowed duration of a BreakRequest referencing
                  this template.
                type: string
              paramSchema:
                description: |-
                  ParamSchema is the JSON Schema used to validate template parameters.
                  Properties may use the x-capsule-form vendor extension to select values
                  from arbitrary Kubernetes GVKs in compatible form clients. The schema may
                  use x-kubernetes-validations for Kubernetes-compatible CEL rules.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              resources:
                description: Resources rendered and managed by this template.
                items:
                  properties:
                    policy:
                      default: {}
                      description: Policy controls how every target in this resource
                        group is created and managed.
                      properties:
                        creation:
                          default: Owner
                          description: |-
                            Creation controls how an existing target is handled. Owner requires the
                            resource to have been created by the applying controller and otherwise
                            returns an error. Merge adopts an existing resource when possible and
                            creates the resource when it does not exist.
                          enum:
                          - Owner
                          - Merge
                          type: string
                        deletion:
                          default: Remove
                          description: |-
                            Deletion controls what happens when the parent stops managing the target.
                            Remove deletes resources created for the parent and relinquishes adopted
                            resources. Orphan keeps the resource and removes Capsule's lifecycle
                            metadata. Removal is the default.
                          enum:
                          - Remove
                          - Orphan
                          type: string
                        force:
                          default: false
                          description: Force allows server-side apply to acquire conflicting
                            field ownership.
                          type: boolean
                        protect:
                          default: true
                          description: |-
                            Protect prevents users from changing or deleting the target through
                            admission while it is managed. Protection is enabled by default.
                          type: boolean
                      type: object
                    targets:
                      description: |-
                        Targets are Kubernetes resource objects to render and manage with Policy.
                        Parameters and loaded context use the flat template root; trusted request
                        metadata is available under .request.
                      items:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      minItems: 1
                      type: array
                    template:
                      description: |-
                        Template is an optional Go template which may render one or more YAML or
                        JSON Kubernetes resources separated by YAML document markers. Request
                        parameters are available under .params and loaded resources under
                        .context.resources. Trusted request metadata is available under .request
                        with name, username, groups, and timestamp fields.
                      minLength: 1
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: at least one target or a template is required
                    rule: (has(self.targets) && size(self.targets) > 0) || has(self.template)
                minItems: 1
                type: array
            required:
            - resources
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
